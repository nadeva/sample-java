name: Test print variable
on: [push]
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Echo variable
        run: echo $TEST_VAR
      - name: Print secret
        run: echo ${{ secrets.SECRET_VAR }}
      - name: testPrint secret
        run: echo $SECRET_VAR


